# REG3 Import-Check Dependencies

# XLSX file handling
openpyxl>=3.1.0

# XML parsing (included in Python stdlib, but listing for clarity)
# xml.etree.ElementTree - built-in

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
